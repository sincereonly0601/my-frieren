---
description: 使用者要求「回到上一版」時須實際還原，不可自行推想替代方案
alwaysApply: true
---

# 還原／上一版

當使用者說「回到上一個版本」「還原」「改回修改前」「undo」等，且意指**撤銷近期變更**時：

1. **必須**以實際存在的舊狀態為準還原（例如 `git checkout`、`git restore`、`git show <rev>:path` 後寫回），**不要**依對話推測「以前大概是什麼樣子」再重寫一版自認等價的程式。
2. 若專案**沒有** Git 或無法取得先前檔案：先說明限制，再請使用者指定還原依據（例如 Cursor Local History、備份路徑、貼上舊片段、或明確檔名＋時間點），**不要**在缺依據時自行發明「上一版」。
3. 還原完成後簡短說明**依據**（哪個 commit、哪個檔案版本），而非只說「已照你的意思改好了」。
