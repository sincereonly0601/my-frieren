---
description: 使用者說「每段X行」時的定義（連寫、視覺行高、不手動換行）
alwaysApply: true
---

# 「每段 X 行」的專案約定

當使用者說**每段 X 行**（或類似說法）時，一律解讀為：

1. **連寫**：該段在資料裡是**連續文字**，段落之間仍可用結構區分（例如多個 `<p>`、多則字串），但**段內不要**為了「湊行數」而插入 `\n`、`white-space: pre-line`、或段內 `<br>`。
2. **不手動換行**：行數來自**瀏覽器／排版引擎**依欄寬、字級、字距的自動折行。
3. **字量目標**：透過**增刪字數**（或微調 `max-width`／字級，若使用者同意動版面）讓內容在**目前約定版面**（例如契約偽古文欄約 `33em`、既有 HUD 字級）下，**大約**呈現 **X 條視覺橫列**；極窄螢幕可略多行，屬可接受範圍。

## 不要做的事

- 不要用 `\n` 或 `pre-line` 把一段**固定**成 X 行來「完成」需求。
- 不要假設使用者要的是「字串裡硬斷 X 行」；除非使用者**另外**明說要固定斷行。

## 實作時可對照

- 網頁契約偽古文：`web/src/story/contractCopy.ts` 的 `CONTRACT_RUNE_LINES_WEB`、`.hud-contract-runes-inner` 欄寬與 `.hud-contract-rune-line` 字級。
